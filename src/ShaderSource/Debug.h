#pragma once

#include "../Recourses.h"

// Basic debug shader

namespace Shaders
{
    namespace Debug
    {
        const char* vert = (const char*)(char*)recourses_Debug_vert;


        const char* frag = (const char*)(char*)recourses_Debug_frag;
    }
}
